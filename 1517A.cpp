#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    while(t--)
    {
        long long int n,p=2050,cnt=0;
        cin>>n;
        int k=13;
        while(n!=0)
        {
            if(p*pow(10,k)<n)
            {
                k--;

            }

        }
    }
}
